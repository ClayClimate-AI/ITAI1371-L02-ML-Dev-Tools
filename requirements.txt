# ITAI-1371 L02 -- ML Development Tools
# Install:  pip install -r requirements.txt
# Any package installed outside this file does not exist. Add it here in the
# same commit that introduces it, or Tech_Spec.md is out of sync with reality.

# --- Core lab stack (Tech_Spec.md section 1) ---
numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# --- Notebook kernel ---
ipykernel>=6.0
notebook>=7.0

# --- Testing (tests/test_pipeline_validator.py) ---
pytest>=7.0

# --- Explicitly NOT included ---
# scikit-learn, seaborn, plotly, torch, tensorflow
# These require a Tech_Spec.md amendment before being added. See Product_Spec.md section 4.
